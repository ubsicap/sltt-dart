name: sltt_dart
description: A comprehensive mono-repo for offline-first sync systems

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

# This is a workspace configuration
# Individual packages have their own dependencies
workspace:
  - packages/sltt_core
  - packages/aws_backend
  - examples/sync_manager_demo
