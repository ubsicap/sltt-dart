name: sync_manager
description: Client-side sync manager with Isar-based storage for SLTT project
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  sltt_core:
    path: ../sltt_core
  isar: ^3.1.0+1
  path: ^1.8.0
  http: ^1.1.0

dev_dependencies:
  build_runner: ^2.4.9
  isar_generator: ^3.1.0+1
  test: ^1.24.0
  lints: ^3.0.0
