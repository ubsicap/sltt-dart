{
  "name": "aws_backend",
  "version": "1.0.0",
  "description": "This package provides AWS DynamoDB storage services for the Dart SLTT (Sign Language Translation Tool) system, optimized for serverless deployment with minimal cold start time.",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "build": "build.bat",
    "build:linux": "rm -f bootstrap && dart compile exe bin/bootstrap.dart -o bootstrap && chmod +x bootstrap",
    "deploy:tst": "npm run build && npx serverless deploy --stage tst --aws-profile sltt-dart-dev && npm run cloudfront-url:tst",
    "deploy:dev": "npm run build && npx serverless deploy --stage dev --aws-profile sltt-dart-dev && npm run cloudfront-url:dev",
    "deploy:prod": "npm run build && npx serverless deploy --stage prod --aws-profile sltt-dart-prd",
    "logs:tst": "npx serverless logs --function api --stage tst --aws-profile sltt-dart-dev --startTime 1h",
    "logs:dev": "npx serverless logs --function api --stage dev --aws-profile sltt-dart-dev --startTime 1h",
    "logs:prod": "npx serverless logs --function api --stage prod --aws-profile sltt-dart-prd --startTime 1h",
    "info:tst": "npx serverless info --stage tst --aws-profile sltt-dart-dev",
    "info:dev": "npx serverless info --stage dev --aws-profile sltt-dart-dev",
    "info:prod": "npx serverless info --stage prod --aws-profile sltt-dart-prd",
    "cloudfront-url:tst": "aws cloudformation describe-stacks --stack-name sltt-backend-tst --query \"Stacks[0].Outputs[?OutputKey=='MediaCloudFrontDomainName'].OutputValue\" --output text --profile sltt-dart-dev",
    "cloudfront-url:dev": "aws cloudformation describe-stacks --stack-name sltt-backend-dev --query \"Stacks[0].Outputs[?OutputKey=='MediaCloudFrontDomainName'].OutputValue\" --output text --profile sltt-dart-dev",
    "cloudfront:generate-rsa-private-key": "openssl genpkey -algorithm RSA -out ../../../private_key.pem -pkeyopt rsa_keygen_bits:2048",
    "cloudfront:generate-rsa-public-key": "openssl rsa -pubout -in ../../../private_key.pem -out ../../../public_key.pem",
    "cloudfront:public-key-set-ssm:tst": "aws ssm put-parameter --name /sltt/media/cloudfront/tst/MEDIA_CLOUDFRONT_PUBLIC_KEY --type String --value file://../../../public_key.pem --overwrite --profile sltt-dart-dev",
    "cloudfront:private-key-set-ssm:tst": "aws ssm put-parameter --name /sltt/media/cloudfront/tst/MEDIA_CLOUDFRONT_PRIVATE_KEY --type SecureString --value file://../../../private_key.pem --overwrite --profile sltt-dart-dev",
    "cloudfront:public-key-set-ssm:dev": "aws ssm put-parameter --name /sltt/media/cloudfront/dev/MEDIA_CLOUDFRONT_PUBLIC_KEY --type String --value file://../../../public_key.pem --overwrite --profile sltt-dart-dev",
    "cloudfront:private-key-set-ssm:dev": "aws ssm put-parameter --name /sltt/media/cloudfront/dev/MEDIA_CLOUDFRONT_PRIVATE_KEY --type SecureString --value file://../../../private_key.pem --overwrite --profile sltt-dart-dev",
    "clean": "rm -f bootstrap && rm -rf .serverless",
  "generate-server-urls": "node ../../scripts/generate_server_urls.js",
    "test": "dart test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless": "^4.4.7"
  }
}
