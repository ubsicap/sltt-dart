name: sltt_core
description: Core sync system for offline-first applications with multi-server architecture

version: 1.0.0+1

environment:
  sdk: '^3.8.2'

resolution: workspace

dependencies:
  # Core dependencies
  uuid: ^4.0.0
  dio: ^5.4.0
  meta: ^1.10.0
  http: ^1.0.0

  # Local persistence
  path: ^1.8.0

  # Shelf for REST API server
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  shelf_cors_headers: ^0.1.3
  shelf_static: ^1.1.0

  # JSON handling
  json_annotation: ^4.8.0
  logging: ^1.0.0

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0

  # Code generation
  build_runner: ^2.4.9
  json_serializable: ^6.8.0
